# 移动端发布操作手册

这个手册用于把电脑上构建好的无名杀产物发布到 GitHub 的 `mobile-dist` 分支，然后手机端通过 `git pull` 同步。

## 目录说明

当前目录结构大致是：

```text
D:\mycode\AI\sgs\noname-backup
├─ noname              源码仓库，主要使用 main 分支
└─ noname-mobile-dist  发布仓库，只放 dist 产物，对应 GitHub 的 mobile-dist 分支
```

平时改代码、构建，都在：

```text
D:\mycode\AI\sgs\noname-backup\noname
```

手机同步用的发布分支内容来自：

```text
D:\mycode\AI\sgs\noname-backup\noname-mobile-dist
```

## PowerShell 和 pwsh 怎么用

`pwsh` 是 PowerShell 7。项目里建议使用 PowerShell 7，不建议用老的 Windows PowerShell 5。

如果你不确定当前窗口是不是 PowerShell 7，可以输入：

```powershell
$PSVersionTable.PSVersion
```

如果 `Major` 是 `7` 或更高，就是 PowerShell 7。

最稳的写法是命令里带上 `pwsh`，例如：

```powershell
pwsh -ExecutionPolicy Bypass -File scripts/publish-mobile-dist.ps1
```

这个写法即使你当前是在普通 PowerShell 窗口里，也会自动调用 PowerShell 7 来运行脚本。

如果你已经确定自己就在 PowerShell 7 里，也可以直接运行：

```powershell
./scripts/publish-mobile-dist.ps1
```

如果直接运行脚本时遇到执行策略拦截，就改回带 `pwsh -ExecutionPolicy Bypass -File` 的写法。

## 一键发布 mobile-dist

进入源码仓库：

```powershell
cd D:\mycode\AI\sgs\noname-backup\noname
```

构建并发布到 GitHub 的 `mobile-dist` 分支：

```powershell
pwsh -ExecutionPolicy Bypass -File scripts/publish-mobile-dist.ps1
```

这个脚本会自动做这些事：

```text
pnpm build
切到 noname-mobile-dist 的 mobile-dist-split 分支
拉取 origin/mobile-dist
清空 noname-mobile-dist 旧内容
复制 noname/dist 到 noname-mobile-dist
跳过 extension/十周年UI/assets/dynamic
提交发布分支
推送到 GitHub 的 mobile-dist 分支
```

注意：脚本会清空电脑上的 `noname-mobile-dist` 再复制新 `dist`，所以不要把私人文件、手机动皮备份放在电脑的 `noname-mobile-dist` 目录里。

## 已经构建过时发布

如果你已经手动执行过 `pnpm build`，只想发布当前 `dist`：

```powershell
pwsh -ExecutionPolicy Bypass -File scripts/publish-mobile-dist.ps1 -SkipBuild
```

## 只测试脚本

只检查路径和配置，不构建、不复制、不提交、不推送：

```powershell
pwsh -ExecutionPolicy Bypass -File scripts/publish-mobile-dist.ps1 -SkipBuild -SkipPush -DryRun
```

只同步并提交到本地发布分支，但不推送 GitHub：

```powershell
pwsh -ExecutionPolicy Bypass -File scripts/publish-mobile-dist.ps1 -SkipPush
```

## 上传源码 main 分支

源码改完并提交后，如果要把 `main` 也上传到你的 GitHub fork：

```powershell
cd D:\mycode\AI\sgs\noname-backup\noname
git push origin main:main
```

如果想以后直接用 `git push` 推到你的 fork 的 `main`：

```powershell
git branch --set-upstream-to=origin/main main
```

之后源码常规提交流程：

```powershell
cd D:\mycode\AI\sgs\noname-backup\noname
git status --short
git add -A
git commit -m "你的提交说明"
git push
```

## 手机端同步

手机第一次 clone：

```bash
git clone --single-branch --branch mobile-dist https://github.com/HERMIT2021/noname.git
```

然后把你已有的动皮资源放回：

```text
extension/十周年UI/assets/dynamic/
```

以后手机更新只做：

```bash
git pull
```

普通 `git pull` 不会删除未被 Git 跟踪的动皮资源。

手机端不要执行这些命令：

```bash
git clean -fd
git clean -fdx
git reset --hard
```

尤其不要执行 `git clean -fdx`，它会删除被 `.gitignore` 忽略的文件，动皮资源可能会被清掉。

如果启动器或同步工具里有这些选项，也不要开启：

```text
清理未跟踪文件
强制镜像同步
删除目标多余文件
重置仓库
```

## 常用命令速查

发布到手机分支：

```powershell
cd D:\mycode\AI\sgs\noname-backup\noname
pwsh -ExecutionPolicy Bypass -File scripts/publish-mobile-dist.ps1
```

跳过构建，只发布已有 `dist`：

```powershell
cd D:\mycode\AI\sgs\noname-backup\noname
pwsh -ExecutionPolicy Bypass -File scripts/publish-mobile-dist.ps1 -SkipBuild
```

只测试：

```powershell
cd D:\mycode\AI\sgs\noname-backup\noname
pwsh -ExecutionPolicy Bypass -File scripts/publish-mobile-dist.ps1 -SkipBuild -SkipPush -DryRun
```

上传源码 `main`：

```powershell
cd D:\mycode\AI\sgs\noname-backup\noname
git push origin main:main
```

## 改武将

**只改显示分类**
如果你只是想让武将包界面里“标准包 / SP / 龙血玄黄”显示得更合理，只改每个包的 sort.js 就行。
比如标准包排序在 standard/sort.js (line 1)，菜单会按这里的分组显示。

这种改法最轻，但它不改变实际加载归属。武将数据仍然属于原来的包，关闭原包时资源和技能仍然受原包影响。

**真正拆包**
如果你希望“关闭标准包时 SP 武将不要跟着加载 / 新增龙血玄黄包可以单独启用关闭”，那就要真正迁移包数据。

主要会动这些地方：

1. 包登记
   game/package.js (line 2)

   这里决定有哪些武将包会被游戏识别。比如新增：

   js

   

   `longxuexuanhuang: "龙血玄黄", sp_common: "SP",`

   注意：现在已有 sp 这个包，但它显示名是“璀璨星河”，所以你如果要新增真正的 SP 包，建议不要直接复用 sp，可以用 sp_common、sp_basic 这种新 ID。

2. 新建包目录
   位置是：

   text

   

   `D:\mycode\AI\sgs\noname-backup\noname\apps\core\character\你的包ID\`

   可以复制 standard 或小一点的包结构，核心文件是：

   text

   

   `index.js character.js skill.js translate.js sort.js voices.js intro.js characterFilter.js characterTitle.js dynamicTranslate.js perfectPairs.js pinyin.js`

   standard/index.js (line 17) 里面的 name: "standard" 和 characterSort: { standard: characterSort } 要改成新包 ID。

3. 迁移武将本体
   每个武将至少要处理：

   text

   

   `character.js        武将定义，性别、势力、体力、技能列表 skill.js            这个武将用到的技能定义 translate.js        武将名、技能名、技能描述 sort.js             武将在包界面里的分组位置 voices.js           台词/语音翻译或映射 intro.js            武将介绍 characterTitle.js   称号 dynamicTranslate.js 动态技能描述 pinyin.js           拼音搜索 perfectPairs.js     珠联璧合 characterFilter.js  模式过滤`

   不是每个武将每个文件都有内容，但迁移时都要搜一遍。

4. 加载机制
   游戏读取包列表的位置在 noname/init/index.ts (line 194)。
   新包只要在 game/package.js 登记，并且目录里有 index.js，就会进入武将包加载逻辑。

5. 菜单显示
   武将包菜单按 characterSort 分组显示，代码在 characterPackMenu.js (line 274)。
   所以 sort.js 里最好这样写：

   js

   

   `const characterSort = {    longxuexuanhuang_default: ["xxx", "yyy"],    longxuexuanhuang_sp: ["aaa", "bbb"], }; const characterSortTranslate = {    longxuexuanhuang_default: "龙血玄黄",    longxuexuanhuang_sp: "龙血玄黄·SP", };`

**最重要的注意点**

不要轻易改武将 ID。比如 old_re_lidian 最好还是叫 old_re_lidian。
因为禁将、斗地主评分、皮肤、语音、存档、扩展配置都可能引用这个 ID。你可以把它从标准包移动到 SP 包，但不要把它改名成 sp_lidian，除非你准备做完整迁移。

技能一定要跟着走。
如果你把武将放进新包，但技能还留在旧包，而旧包又被关闭，新包里的武将就会出现技能不存在、AI 异常、报错或白板。

不要改 dist。
你只改：

text



```
D:\mycode\AI\sgs\noname-backup\noname\apps\core\character D:\mycode\AI\sgs\noname-backup\noname\apps\core\game\package.js
```

dist 让 pnpm build 重新生成。

**推荐你的手工流程**

先新增一个空包，比如 sp_common，确认游戏里能看到“SP”包。
然后只迁移 1 个武将试水，比如你认为标准包里不该存在的某个武将。迁移后测试：启用新包、关闭旧包，看这个武将能不能正常选、正常出技能、无控制台报错。

查某个武将在哪些地方出现，可以用：

powershell



```
pwsh -Command "Get-ChildItem -LiteralPath 'apps/core/character' -Recurse -File -Include '*.js' | Select-String -Pattern 'old_re_lidian'"
```

查某个技能在哪些地方出现：

powershell



```
pwsh -Command "Get-ChildItem -LiteralPath 'apps/core/character' -Recurse -File -Include '*.js' | Select-String -Pattern 'retieji'"
```

做这种大迁移，我建议你自己维护一个表：

text



```
武将ID | 原包 | 新包 | 技能列表 | character已移 | skill已移 | translate已移 | sort已移 | 已测试
```

最后每迁移一批就跑：

powershell



```
pwsh -Command "pnpm build"
```

然后进游戏手测武将包界面、选将、技能发动。这个活最怕一次搬几十个，出错后不知道是哪一个引起的。先 1 个，再 5 个一批，会稳很多。
